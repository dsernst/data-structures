describe('redBlackTree', function() {
  var p;

  beforeEach(function() {
    p = new PrefixTree();
  });

  xit('should allow inserts', function() {
    b.insert(50);
    expect(b.value).to.equal(30);

  });


});
